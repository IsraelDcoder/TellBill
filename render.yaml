services:
  - type: web
    name: tellbill-backend
    env: node
    buildCommand: npm ci --include=dev && npm run build && echo "Build complete"
    startCommand: node server_dist/index.js
    envVars:
      - key: NODE_ENV
        value: production

